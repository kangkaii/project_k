package kk.practise.递归;

/**
 * @author by kangkai on 2017/10/17.
 */
public class ArrayTest {


    /*public void test() {
        //计算 一个数组里面 有多少非0 的数据
        int[] array = new int[]{1,2,3,4,5,6};


    }

    public int getByIteration(int[] array) {//迭代实现
        int result = 0;
        for (int i = 0; i < array.length; i++) {
            if (array[i] != 0) {
                result++;
            }
        }
        return result;
    }

    public int getByRecursion(int[] array) {//递归实现
        int result = 0;
        int length = array.length;
        if (i == 0) {
            return result;
        }else {
            i--;
            if (array[i] != 0) {
                result++;
            }
        }

        return result;
    }*/

}
